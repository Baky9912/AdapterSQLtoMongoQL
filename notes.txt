/*
 * 
 * ( SELECT * FROM a WHERE (SELECT max(x) from a) GROUP_BY z )
 * 
 */

 /*
  * 
    podupit
    literal (7 "Jovan")
    kolona
    komanda


    select a, b, avg(c) from x where a in (select distinct(a) from y)
    
    avg
    (
    c
    )


    where, having - uslov validator
    select - lista fieldova i agg funkcija

  */